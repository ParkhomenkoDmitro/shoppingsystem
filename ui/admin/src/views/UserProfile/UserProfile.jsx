import React from 'react';

function UserProfile({ ...props }) {
    return (
        <div>
            <p>Hello from user profile component !</p>
        </div>
    );
};

export default UserProfile;

